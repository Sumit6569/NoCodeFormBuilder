# MongoDB Configuration
# For MongoDB Atlas (recommended - already configured):
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.slpluqj.mongodb.net/forms-builder?retryWrites=true&w=majority&appName=Cluster0

# For local MongoDB (alternative):
# MONGODB_URI=mongodb://localhost:27017/forms-builder

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (optional)
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000
